# 公司 Wiki 建设说明

## 资料边界

- 唯一原始资料目录为 `company-handbook/`，Agent 只能读取，不能修改。
- Agent 输出全部进入 `generated-wiki/drafts/`，默认状态为 `draft`。
- 草稿经人工审核后才能进入未来的发布流程；本阶段不自动发布。

## 首批建设原则

- 每次 `init` 只处理命令中 `--scope` 指定的范围。
- 未指定范围时，优先建立总入口和公司内部管理制度导航，不展开全部外部政策。
- 先覆盖员工高频问题对应的制度，再逐步建设外部政策资料。
- 不按“一份源文件对应一篇页面”机械拆分，而是按可回答的问题和业务主题组织。

## 内容要求

- 金明额、日期、期限、流程、条件和处罚必须标来源。
- 冲突、过期风险和无法确认的信息必须进入 `uncertainties`。
- 身份证号、私人联系方式、银行卡号和个人薪资明细不得进入通用 Wiki。
