运行根目录：`$runtime_root`

资料源：`$source_dir`

Wiki 草稿输出：`$draft_dir`

本次处理范围：

$scope

用户补充要求：

$user_message

本次资料文件清单：

```json
$sources
```

必须为清单中的每份源文件创建一个独立页面。页面目录应镜像源文件目录，文件名
使用源文件标题；`scope` 只代表内容提炼重点，不代表允许遗漏资料。所有结论必须以
实际读取的唯一对应源文件为依据。
